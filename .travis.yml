script: cp "Användbara bilder/Mobil.html" "Användbara bilder/index.html"

deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    local-dir: 'Användbara bilder'
    on:
        branch: master
